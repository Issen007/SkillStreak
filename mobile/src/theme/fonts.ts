// Font family names from docs/design/style-guide.md (v1, Phase 0.5).
// Loaded via @expo-google-fonts/baloo-2 and @expo-google-fonts/nunito;
// the string values below must match the keys passed to useFonts() in App.tsx.
export const fonts = {
  /** Baloo 2 SemiBold+ — headings only. */
  headingBold: 'Baloo2_700Bold',
  /** Nunito Regular — body/labels. */
  body: 'Nunito_400Regular',
  /** Nunito Bold — emphasis within body/labels. */
  bodyBold: 'Nunito_700Bold',
} as const;
